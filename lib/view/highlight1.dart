import 'package:flutter/material.dart';

class FirstHighlightPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('第一個影片精華'),
      ),
      body: Center(
        child: Text('這是第一個影片精華的頁面'),
      ),
    );
  }
}
