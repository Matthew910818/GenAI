import 'package:flutter/material.dart';

class SecondHighlightPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('第二個影片精華'),
      ),
      body: Center(
        child: Text('這是第二個影片精華的頁面'),
      ),
    );
  }
}
