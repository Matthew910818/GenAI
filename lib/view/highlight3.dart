import 'package:flutter/material.dart';

class ThirdHighlightPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('第三個影片精華'),
      ),
      body: Center(
        child: Text('這是第三個影片精華的頁面'),
      ),
    );
  }
}
